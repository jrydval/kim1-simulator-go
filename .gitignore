# Build artifacts
/bin/
/dist/
*.exe
*.test
*.out

# Go workspace/build cache leftovers
*.o
*.a

# OS cruft
.DS_Store

# User-supplied KIM-1 ROM dumps — third-party copyrighted content, never
# committed (unlike testdata/6502_functional_test.bin, which is the
# author-published Dormann test binary — see testdata/README.md).
testdata/6530-*.bin

# Third-party KIM-1 software (paper-tape/hex program images) dropped into
# testdata/ for local testing — same reasoning as the ROM dumps above.
testdata/*.ptp
testdata/*.hex

# Editor
.vscode/*
!.vscode/tasks.json
.claude/settings.local.json
